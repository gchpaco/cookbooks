server {
  listen   80;
  server_name  <%= node[:hostname] %>;

  access_log  <%= node[:nginx][:log_dir] %>/<%= node[:hostname] %>.access.log;

  location / {
    root /var/www/nginx-default;
    index index.html index.htm;
    proxy_pass http://127.0.0.1:<%= node[:nginx][:proxy_port] %>/;
  }
}
